
<%= form_for :proposal, url: proposals_path do |f| %>
    <p>
      <%= f.label :idea %><br>
      <%= f.text_area :idea %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>

<%= link_to 'Back', proposals_path %>